configuration:
  externalHost: aka.devel
  adminGroups:
    - open-source
postgresql:
  existingSecret:
    name: postgres-dsn
    key: dsn
api:
  image:
    pullPolicy: Never
ui:
  image:
    tag: main
oauth2proxy:
  env:
    - name: OIDC_OPTIONS_ISSUER_URI
      value: https://gitlab.dcas.dev
    - name: OIDC_OPTIONS_SCOPES
      value: "openid,profile,email"
    - name: OIDC_OPTIONS_CLIENT_ID
      valueFrom:
        secretKeyRef:
          name: oidc
          key: OIDC_CLIENT_ID
    - name: OIDC_OPTIONS_CLIENT_SECRET
      valueFrom:
        secretKeyRef:
          name: oidc
          key: OIDC_CLIENT_SECRET
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: iss-kube
  tls:
    - secretName: tls-aka
      hosts:
        - aka.devel